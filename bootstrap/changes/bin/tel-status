#!/data/data/com.termux/files/usr/bin/sh

if [ "$#" != "2" ]; then
	echo 'usage: tel-status 0 "text"'
	exit
fi
am broadcast --user 0 \
		--es text $2 \
		--es line $1 \
		-a com.termux.app.status com.termux > /dev/null
