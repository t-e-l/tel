#!/data/data/com.termux/files/usr/bin/sh

if [ "$#" != "0" ]; then
	echo 'usage: tel-api'
	exit
fi

am broadcast --user 0 \
		 --es action testdatt \
		 --ei time 33 \
		 -a com.termux.app.api com.termux > /dev/null
